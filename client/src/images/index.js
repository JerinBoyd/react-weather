import rain from './rain.png';


const selection = (name) => {
    switch(name){
        case "rain":
           return rain;
        default:
           return rain;
    }
    if(name === 'rain') { 
        return rain;
    }
    return rain;
}

export default selection;